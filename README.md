# Asteroid
Asteroid game built in C, (Made with Raylib)
Feature a built in editor to modify asteroids properties and a preset selector
Setup
=====
for now *Raylib* and *Raygui* are fetched with the cmake so everything **should** be fine but the cmake will need to include the requirements for apple